#include <sdtio.h>
#include <string.h>
#include <ctype.h>
#include "parser.h"

void cortar(char *string)
{
    char *fim;

    while (isspace ((unsigned char) == string)) string++;
        if (*string == 0) return NULL;

}